# Raw Data

Place the raw coursework input files in this directory before running the reproduction pipeline:

- `prices.parquet`
- `earnings_calendar.parquet`
- `short_interest_transfo.parquet`
- `sp500_tr.parquet`

The repository uses these files as immutable inputs. All derived artifacts are written under `outputs/`.
